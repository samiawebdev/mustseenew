
const Toasterator = /*({state, message}*/ /*: {state: string, message: string})*/ () => {

    // const toast = state == "ok" ?;
    return (

        <>

        </>

    )
}

export default Toasterator
